package com.example.pro.model;

/**
 * Lifecycle status of an {@link com.example.pro.entity.Appointment}.
 */
public enum AppointmentStatus {
    /** The appointment is confirmed and active. */
    BOOKED,
    /** The appointment has been cancelled. */
    CANCELLED
}
